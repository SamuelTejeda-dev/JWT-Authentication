//questo enum è usato per definire gli errore dell'applicazione, in modo da avere un codice di errore univoco per ogni errore

const enum AppErrorCode {
  InvalidAccessToken = "InvalidAccessToken",
}

export default AppErrorCode;
